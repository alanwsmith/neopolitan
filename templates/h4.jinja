{% from "text.jinja" import text -%}
{% macro h4 (data) -%}
{%- for block in data.children -%}
{%- if loop.first -%}
<h4
  {%- for attribute in data.attributes -%}
    {{ " " }}{{ attribute.key }}="{{attribute.value}}"
{%- endfor -%}>{{- text(block) -}}</h4>
{% else -%}
<p>{{- text(block) -}}</p>
{% endif -%}
{%- endfor -%}
{%- endmacro -%}
