<!DOCTYPE html>

<html>

<head>
  <title>{{ title }}</title>
  <style>
    body {
      background-color: #000;
      color: #aaa;
    }
  </style>
</head>

<body>
  <h1>{{ title }}</h1>
  <ul>
    {% for post in posts %}
    <li><a href="{{ post[1] }}">{{ post[0] }}</a></li>
    {% endfor %}
  </ul>
</body>

</html>
